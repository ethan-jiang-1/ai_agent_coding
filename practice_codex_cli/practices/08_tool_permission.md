# 08 工具权限克制：默认最小权限，按阶段抬升

## 核心习惯

在 Codex CLI 里，权限不是一个开关，而是一套工作流设计。先用只读，再给工作区写权限，最后才考虑更激进的自动化。

## 在 Codex CLI 里怎么做

- 规划、审计、定位问题时，先用 `read-only`。
- 真正开始改文件时，再切到 `workspace-write`。
- 自动化执行时，优先使用 `--full-auto` 这种受控快捷方式，而不是直接绕过所有保护。
- 如果你已经在交互式会话里，`/approval-mode` 是最直接的权限控制入口，`/approvals` 是它的隐藏别名，`/status` 适合做状态确认。

还要注意一个现实差异：

- 交互式 `codex` / `resume` / `fork` 有更完整的 approval 控制
- 当前 `codex exec` 暴露的是沙箱级别和自动化快捷方式，不要想当然把交互式参数写到 `exec` 上

## 权限提升的顺序

1. 只读分析
2. 工作区内写入
3. 低摩擦自动执行
4. 极危险绕过模式

如果你还没看清任务边界，就不该跳到第 3 或第 4 层。

## 不要这样做

- 不要一上来就是危险绕过模式。
- 不要把交互式的参数习惯直接搬到 `codex exec`。
- 不要把“能写工作区”理解成“改什么都安全”。

## 验收标准

- 当前命令的权限级别和任务阶段匹配。
- 规划阶段没有不必要的写权限。
- 自动化命令没有偷偷升到超出需要的权限。

## 对应支撑

见 `commands/08_sandbox_and_approval_commands.md`。
